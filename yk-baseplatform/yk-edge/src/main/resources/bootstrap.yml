spring:
  application:
    name: edge
encrypt:
  failOnError: false
  
---
spring:
  profiles: dev
  application:
    name: edge
  cloud:
    config:
      uri: http://${YK_CONFIG_SERVICE_HOST}:${YK_CONFIG_SERVICE_PORT}
encrypt:
  failOnError: false